# Evidencia de CA-RF03-01

## Alcance verificado

CA-RF03-01 exige que una búsqueda normalizada, sin distinguir mayúsculas de
minúsculas, encuentre únicamente productos disponibles que coincidan en
`name`, `summary` o `description`.

## Regresión observada

Con PostgreSQL disponible, el 2026-09-02 se ejecutó:

```bash
.venv/bin/python -m pytest -q \
  apps/catalog/tests/test_views.py::test_catalog_searches_available_products_by_normalized_text_case_insensitively
```

Resultado observado: `1 passed in 0.51s`.

No se creó un estado Red en este cierre porque no se modificó comportamiento de
producción. El ciclo test-first original pertenece al commit de implementación.

## Trazabilidad

- Requisito: RF-03.
- Criterio: CA-RF03-01.
- Prueba: `test_catalog_searches_available_products_by_normalized_text_case_insensitively`.
- Implementación: `apps/catalog/views.py:product_list` y
  `apps/catalog/selectors.py:get_available_products`.
- Commit de comportamiento: `3e52b90`.

## Alcance excluido

Esta evidencia no acredita filtros, ordenación, paginación, estado vacío ni
validación de otros parámetros de RF-03.